name: Build

on:
  pull_request:
  push:
    branches:
    tags:
      - "!*"

jobs:
  build:
    runs-on: ubuntu-18.04

    strategy:
      matrix:
        node: [8, 10, 12]

    steps:
      - uses: actions/checkout@master
      - run: npm install
      - run: npm run lint
      - run: npm run build
      - run: npm run test
