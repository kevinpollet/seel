{
  "name": "express",
  "description": "An Express application that serves static files and exposes a REST endpoint",
  "version": "1.0.0",
  "author": "kevinpollet <pollet.kevin@gmail.com>",
  "license": "MIT",
  "private": true,
  "bin": "bin/server.js",
  "main": "lib/index.js",
  "scripts": {
    "build:seel": "npm run --prefix ../../ build",
    "build:image": "npm run build:seel && ../../bin/seel.js --port 3000 --extra-files 'public/**'"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
